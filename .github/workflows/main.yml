# This file is derived from Qiskit Finance for use in FinQIR.
#
# (C) Copyright IBM 2021, 2024.
#
# This code is licensed under the Apache License, Version 2.0. You may
# obtain a copy of this license in the LICENSE.txt file in the root directory
# of this source tree or at http://www.apache.org/licenses/LICENSE-2.0.
#
# Any modifications or derivative works of this code must retain this
# copyright notice, and modified files need to carry a notice indicating
# that they have been altered from the originals.

name: FinQIR CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12", "3.14"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install FinQIR
        run: python -m pip install -e ".[test,dev]"
      - name: Check dependencies
        run: python -m pip check
      - name: Run unit tests
        run: python -m unittest discover -s test -v
      - name: Check formatting
        run: python -m black --check finqir test tools docs
      - name: Run static analysis
        run: |
          python -m pylint --persistent=n -rn finqir test tools
          python -m mypy finqir test tools
      - name: Check source headers
        run: python tools/verify_headers.py finqir test tools

  platform:
    name: ${{ matrix.os }} / Python 3.12
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -e ".[test]"
      - run: python -m unittest discover -s test -v

  minimum-dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.10"
      - run: python -m pip install "numpy==2.0.0" "scipy==1.14.0" "qiskit==2.5.2" "qiskit-addon-opt-mapper==0.1.0"
      - run: python -m pip install -e ".[test]"
      - run: python -m unittest discover -s test -v

  qiskit-prerelease:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - run: python -m pip install -e ".[test]"
      - run: python -m pip install --pre --upgrade "qiskit<3" "qiskit-addon-opt-mapper<0.2"
      - run: python -m unittest discover -s test -v

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install build tools
        run: python -m pip install build twine
      - name: Build distributions
        run: python -m build
      - name: Validate distributions
        run: python -m twine check dist/*
      - uses: actions/upload-artifact@v7
        with:
          name: finqir-distributions
          path: dist/
